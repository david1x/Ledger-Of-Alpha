---
phase: 08-trading-tools-hub
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - components/tools/RRCalculator.tsx
  - components/tools/GrowthCalculator.tsx
autonomous: true
requirements:
  - TOOLS-02
  - TOOLS-03

must_haves:
  truths:
    - "User can enter entry, stop, and target prices with a long/short toggle and see a vertical price ladder with colored risk/reward zones"
    - "User can expand a position sizing section to see dollar risk, dollar reward, and recommended shares"
    - "User can enter starting balance, return rate, and period and see a compound growth area chart with final balance"
  artifacts:
    - path: "components/tools/RRCalculator.tsx"
      provides: "R:R calculator with price ladder visualization and collapsible position sizing"
      min_lines: 100
    - path: "components/tools/GrowthCalculator.tsx"
      provides: "Compound growth calculator with Recharts area chart"
      min_lines: 60
  key_links:
    - from: "components/tools/RRCalculator.tsx"
      to: "lib/calculators.ts"
      via: "import riskReward, positionSize"
      pattern: "import.*riskReward.*calculators"
    - from: "components/tools/GrowthCalculator.tsx"
      to: "lib/calculators.ts"
      via: "import compoundGrowthCurve"
      pattern: "import.*compoundGrowthCurve.*calculators"
    - from: "components/tools/GrowthCalculator.tsx"
      to: "recharts"
      via: "AreaChart component"
      pattern: "import.*AreaChart.*recharts"
---

<objective>
Replace the R:R and Compound Growth stub components with full implementations. The R:R calculator features a vertical price ladder visualization with colored risk/reward zones and a collapsible position sizing section. The Growth calculator features a Recharts area chart showing the compound growth curve.

Purpose: Delivers the two most visually rich calculators — both require more than simple input/output tables.
Output: Two fully functional calculator components replacing their stubs.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-trading-tools-hub/08-CONTEXT.md
@.planning/phases/08-trading-tools-hub/08-RESEARCH.md
@.planning/phases/08-trading-tools-hub/08-01-SUMMARY.md

@lib/calculators.ts
@components/RiskCalculator.tsx
@components/PositionSizer.tsx
@components/dashboard/ChartWidgets.tsx
</context>

<interfaces>
<!-- Contracts from Plan 01 that this plan depends on -->

From lib/calculators.ts (created in Plan 01):
```typescript
export function riskReward(
  entry: number, stopLoss: number, takeProfit: number, direction: "long" | "short"
): { riskPerShare: number; rewardPerShare: number; rrRatio: number; riskPct: number; rewardPct: number };

export function positionSize(
  accountSize: number, riskPercent: number, entry: number, stopLoss: number, commission?: number
): { dollarRisk: number; shares: number; positionValue: number };

export function compoundGrowthCurve(
  startBalance: number, monthlyReturnPct: number, months: number
): { month: number; balance: number }[];
```

From components/dashboard/ChartWidgets.tsx:
```typescript
export const TOOLTIP_STYLE = { background: "#0f172a", border: "1px solid #1e293b", ... };
export const TOOLTIP_LABEL_STYLE = { color: "#e2e8f0" };
export const TOOLTIP_ITEM_STYLE = { color: "#e2e8f0" };
export const GRID_STROKE = "#1e293b";
export const TICK = { fontSize: 11, fill: "#64748b" };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build R:R Calculator with price ladder and position sizing</name>
  <files>components/tools/RRCalculator.tsx</files>
  <action>
Replace the stub in `components/tools/RRCalculator.tsx` with the full implementation. "use client" component.

**Inputs (all controlled state with defaults):**
- Entry price (default 100)
- Stop loss price (default 95)
- Take profit price (default 110)
- Direction toggle: Long / Short (default "long") — styled as a segmented button pair (matches trade modal pattern)

**Live recalculation:** Call `riskReward(entry, stop, target, direction)` from `lib/calculators.ts` via `useMemo`. Recalculate on every input change.

**Price Ladder Visualization (main visual — per user decision: "vertical price ladder with colored zones"):**
- Fixed-height container (~300px) with `position: relative`
- Three horizontal lines at computed vertical positions: entry (slate-400), stop (red-500), target (emerald-500)
- Each line has a label to its right showing the price
- Colored zone between entry and stop: `bg-red-500/20` (risk zone)
- Colored zone between entry and target: `bg-emerald-500/20` (reward zone)
- Position mapping: normalize prices to percentages within [minPrice, maxPrice] range, then position as `bottom: X%` within the container (higher price = higher position)
- For SHORT direction: stop is ABOVE entry, target is BELOW — zones flip accordingly

**Stats display below ladder:**
- R:R Ratio (e.g., "3.00 : 1") — large text
- Risk per share, Reward per share
- Risk %, Reward % (relative to entry)

**Collapsible position sizing section (per user decision: "optional collapsible"):**
- Disclosure/accordion below the stats — collapsed by default
- When expanded, two additional inputs: Account size ($, default 10000), Risk % (default 1)
- Call `positionSize(accountSize, riskPct, entry, stop)` from `lib/calculators.ts`
- Display: Total $ risk, Total $ reward, Recommended shares, Position value

**Styling:** Dark-first Tailwind. Consistent with other calculator components from Plan 01.
  </action>
  <verify>
    <automated>cd D:/Code/Projects/claude-test/Ledger-Of-Alpha && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>R:R calculator shows vertical price ladder with colored risk/reward zones. Long/Short toggle changes zone positions. Position sizing section expands to show shares recommendation. All values update live on input change.</done>
</task>

<task type="auto">
  <name>Task 2: Build Compound Growth Calculator with Recharts area chart</name>
  <files>components/tools/GrowthCalculator.tsx</files>
  <action>
Replace the stub in `components/tools/GrowthCalculator.tsx` with the full implementation. "use client" component.

**Inputs (controlled state with defaults):**
- Starting balance ($ — default 10000)
- Monthly return rate (% — default 5)
- Period in months (number — default 12)

**Live recalculation:** Call `compoundGrowthCurve(startBalance, rate, months)` from `lib/calculators.ts` via `useMemo`.

**Recharts Area Chart (per user decision: "Recharts area chart matching dashboard patterns"):**
- Import `{ AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer }` from "recharts"
- Import `{ TOOLTIP_STYLE, GRID_STROKE, TICK }` from `@/components/dashboard/ChartWidgets`
- Chart height: 250px via ResponsiveContainer
- X axis: month number, formatted as "Mo 0", "Mo 6", etc.
- Y axis: dollar amounts formatted with `$` prefix and `toLocaleString()`
- Area: stroke emerald-400 (#34d399), fill with linear gradient (emerald 30% opacity at top, 0% at bottom)
- Define gradient in `<defs>` inside AreaChart (same pattern as existing dashboard charts)
- Grid: dashed, vertical=false, stroke from GRID_STROKE

**Summary stats below chart:**
- Final balance: `$X,XXX.XX` — large green text
- Total gain: `$X,XXX.XX` (final - starting)
- Total gain %: `XX.XX%`
- Show these in a flex row with slate card backgrounds

**Styling:** Dark-first Tailwind. Match the dashboard chart visual language.
  </action>
  <verify>
    <automated>cd D:/Code/Projects/claude-test/Ledger-Of-Alpha && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Growth calculator shows area chart that updates live as inputs change. Summary stats show final balance, total gain ($), and total gain (%). Chart uses emerald gradient matching dashboard style.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. /tools?tab=rr — enter entry=100, stop=95, target=115 for long: see price ladder with green reward zone above entry, red risk zone below; R:R shows 3:1
3. /tools?tab=rr — toggle to Short: zones and stats recalculate correctly
4. /tools?tab=rr — expand position sizing: see recommended shares for $10,000 account at 1% risk
5. /tools?tab=growth — enter $10,000, 5%, 24 months: see smooth area chart with growth curve; final balance ~$32,251
</verification>

<success_criteria>
- R:R calculator displays vertical price ladder with colored zones for both long and short directions
- Position sizing section is collapsible and shows correct share recommendations
- Growth calculator renders Recharts area chart with emerald gradient
- Growth summary shows final balance, total gain $, and total gain %
- Both calculators recalculate live on input change
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-trading-tools-hub/08-02-SUMMARY.md`
</output>
