---
phase: 28-layout-persistence-and-migration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/DashboardShell.tsx
autonomous: true
requirements:
  - PERSIST-01
  - PERSIST-02

must_haves:
  truths:
    - "User resizes cards, refreshes page, sees the same layout they left"
    - "Previously saved user templates load correctly with dims converted to 24-col scale"
    - "Built-in default templates render correctly in the new format"
    - "Saving a new template preserves the _gridScale marker so it loads without re-migration"
  artifacts:
    - path: "components/dashboard/DashboardShell.tsx"
      provides: "Shared migration helper, template load migration, template save stamping"
      contains: "migrateDimsTo24Col"
  key_links:
    - from: "handleLoadTemplate"
      to: "migrateDimsTo24Col"
      via: "function call before applying template dims"
      pattern: "migrateDimsTo24Col"
    - from: "handleSaveTemplate"
      to: "_gridScale"
      via: "stamp GRID_COLS on saved template layout"
      pattern: "_gridScale.*GRID_COLS"
---

<objective>
Extract duplicated layout migration logic into a shared helper and apply it in template load/save paths so that user-saved templates from the old schema render correctly and new templates are stamped for future-proof loading.

Purpose: Close the template migration gap — main layout load already migrates, but handleLoadTemplate and the initial template parse do not. This is the last piece for full persistence across the 6-col to 24-col migration.

Output: Updated DashboardShell.tsx with shared migration helper, template load migration, and template save stamping.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-layout-persistence-and-migration/28-CONTEXT.md

<interfaces>
<!-- Key types and contracts from DashboardShell.tsx -->

From components/dashboard/DashboardShell.tsx:
```typescript
type WidgetDims = { w: number; h: number };

interface DashboardLayout {
  order: string[];
  hidden: string[];
  dims: Record<string, WidgetDims>;
  _gridScale?: number; // 24 = current 24-col scale; absent = needs migration
}

interface LayoutTemplate {
  id: string;
  name: string;
  layout: DashboardLayout;
  createdAt: string;
}

const GRID_COLS = 24;
```

Migration logic currently inline at lines 425-448 (main layout load) and lines 461-483 (admin template load). Both do the same thing:
- `sizes` string format -> 24-col dims (large/normal=12, medium=8, compact=4)
- `dims` without `_gridScale` -> detect scale by maxW (<=6 means x4, <=12 means x2)
- `dims` with `_gridScale === GRID_COLS` -> use as-is

handleLoadTemplate (line 568) does NO migration — just copies dims directly.
handleSaveTemplate (line 550) does NOT stamp `_gridScale` on the template layout.
Template initial parse (line 453-457) loads templates raw with no migration.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract migration helper and wire into template paths</name>
  <files>components/dashboard/DashboardShell.tsx</files>
  <action>
1. **Extract a shared migration helper** called `migrateDimsTo24Col` near the top of DashboardShell.tsx (after the GRID_COLS constant, before the component). It should accept a parsed layout object (with optional `sizes`, `dims`, `_gridScale` fields) and return a clean `Record<string, WidgetDims>` in 24-col scale. The logic is already implemented inline in two places — consolidate:
   - If `sizes` exists and no `dims`: map string values (large/normal -> w:12, medium -> w:8, compact -> w:4, all h:4)
   - If `dims` exists with `_gridScale === GRID_COLS`: use as-is, defaulting missing w/h to 4
   - If `dims` exists without matching `_gridScale`: detect scale by maxW (<=6 -> x4, <=12 -> x2, else x1), multiply all w and h values
   - If neither `sizes` nor `dims`: return empty object (caller will merge with DEFAULT_DIMS)

2. **Replace the inline migration** in the main layout load block (lines ~425-448) with a call to `migrateDimsTo24Col(parsed)`.

3. **Replace the inline migration** in the admin_default_templates block (lines ~461-483) with a call to `migrateDimsTo24Col(override)`.

4. **Add migration to handleLoadTemplate** (line 568): Before applying the template, run `migrateDimsTo24Col(template.layout)` to produce the dims. This handles old user-saved templates that were saved before the 24-col migration. The result should be merged with DEFAULT_DIMS (same as main layout load does) so new widgets added after the template was saved get default sizes.

5. **Stamp _gridScale on handleSaveTemplate** (line 550): When creating the `newTemplate` object, include `_gridScale: GRID_COLS` in the layout so saved templates self-identify as 24-col scale and skip migration on future loads.

6. **Stamp _gridScale on handleSaveAsCopy** (line 589): Same treatment — include `_gridScale: GRID_COLS` in the copied layout.

Do NOT change save timing, debounce behavior, or any other persistence logic. Only the migration extraction and template path fixes.
  </action>
  <verify>
    <automated>cd D:/Code/Projects/claude-test/Ledger-Of-Alpha && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
- migrateDimsTo24Col helper exists and is used in all three migration sites (main layout load, admin template load, user template load)
- handleLoadTemplate applies migration before setting layout
- handleSaveTemplate and handleSaveAsCopy stamp _gridScale: GRID_COLS on saved template layouts
- No inline migration duplication remains
- Build passes with no TypeScript errors
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes — no TypeScript errors
2. Manual smoke test: load dashboard, resize a card, refresh — layout persists (PERSIST-01)
3. If user has old-format saved templates: load one — it renders in 24-col scale (PERSIST-02)
4. Save a new template, inspect the stored JSON — it contains `_gridScale: 24`
</verification>

<success_criteria>
- All layout migration logic consolidated into a single reusable helper
- User-saved templates from any era (string sizes, 6-col dims, 12-col dims, 24-col dims) load correctly
- New templates saved with _gridScale marker for future-proof loading
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/28-layout-persistence-and-migration/28-01-SUMMARY.md`
</output>
