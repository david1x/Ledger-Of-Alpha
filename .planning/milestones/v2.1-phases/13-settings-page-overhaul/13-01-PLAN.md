---
phase: 13-settings-page-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/settings/types.ts
  - components/settings/tabs/AccountTab.tsx
  - components/settings/tabs/AccountsTab.tsx
  - components/settings/tabs/TagsTab.tsx
  - components/settings/tabs/TemplatesTab.tsx
  - components/settings/tabs/AppearanceTab.tsx
  - components/settings/tabs/ChartTab.tsx
  - components/settings/tabs/StrategiesTab.tsx
  - components/settings/tabs/IntegrationsTab.tsx
  - components/settings/tabs/BrokerTab.tsx
  - components/settings/tabs/SecurityTab.tsx
  - components/settings/tabs/DataTab.tsx
  - components/settings/tabs/AdminUsersTab.tsx
  - components/settings/tabs/AdminSettingsTab.tsx
  - app/settings/page.tsx
autonomous: true
requirements:
  - SETT-02

must_haves:
  truths:
    - "Each settings tab renders identically to before the refactor"
    - "Editing one tab's state does not affect another tab's state"
    - "The settings page builds without TypeScript errors"
  artifacts:
    - path: "components/settings/types.ts"
      provides: "Shared types: Settings, Category, CategoryDef, CATEGORIES, AdminUser, SystemSettings, SYS_DEFAULTS, CSS class constants"
      exports: ["Settings", "Category", "CategoryDef", "CATEGORIES", "AdminUser", "SystemSettings", "SYS_DEFAULTS", "INPUT", "LABEL", "HINT"]
    - path: "components/settings/tabs/AccountTab.tsx"
      provides: "Account settings (balance, risk, commission, loss limit, Monte Carlo)"
    - path: "components/settings/tabs/BrokerTab.tsx"
      provides: "IBKR broker connection, account mapping, sync"
    - path: "components/settings/tabs/StrategiesTab.tsx"
      provides: "DnD strategy list with checklist items"
    - path: "app/settings/page.tsx"
      provides: "Thin shell: sidebar + conditional tab rendering"
      min_lines: 50
  key_links:
    - from: "app/settings/page.tsx"
      to: "components/settings/tabs/*Tab.tsx"
      via: "conditional rendering {active === 'X' && <XTab />}"
      pattern: "active === "
    - from: "components/settings/tabs/*Tab.tsx"
      to: "components/settings/types.ts"
      via: "import shared types and constants"
      pattern: "from.*settings/types"
    - from: "components/settings/tabs/*Tab.tsx"
      to: "/api/settings"
      via: "each tab fetches and saves its own settings slice"
      pattern: "fetch.*api/settings"
---

<objective>
Decompose the 2380-line settings monolith (`app/settings/page.tsx`) into a shared types file, 13 per-tab components, and a thin shell page.

Purpose: Satisfy SETT-02 (per-tab components with independent state). This is the foundation for plan 02 (layout, rename, dirty indicator).
Output: 15 files -- `components/settings/types.ts`, 13 tab components in `components/settings/tabs/`, and a rewritten `app/settings/page.tsx` (~120 lines).
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-settings-page-overhaul/13-RESEARCH.md
@app/settings/page.tsx
@lib/types.ts
@lib/account-context.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared types and extract all 13 tab components</name>
  <files>
    components/settings/types.ts,
    components/settings/tabs/AccountTab.tsx,
    components/settings/tabs/AccountsTab.tsx,
    components/settings/tabs/TagsTab.tsx,
    components/settings/tabs/TemplatesTab.tsx,
    components/settings/tabs/AppearanceTab.tsx,
    components/settings/tabs/ChartTab.tsx,
    components/settings/tabs/StrategiesTab.tsx,
    components/settings/tabs/IntegrationsTab.tsx,
    components/settings/tabs/BrokerTab.tsx,
    components/settings/tabs/SecurityTab.tsx,
    components/settings/tabs/DataTab.tsx,
    components/settings/tabs/AdminUsersTab.tsx,
    components/settings/tabs/AdminSettingsTab.tsx
  </files>
  <action>
    **Step 1: Create `components/settings/types.ts`**

    Extract from `app/settings/page.tsx` (lines 31-108):
    - `Settings` interface (all 26 fields, lines 31-57)
    - `TwoFactorSetup` interface (lines 59-62)
    - `Category` type union (line 64)
    - `CategoryDef` interface (line 66)
    - `CATEGORIES` array (lines 66-80) -- keep `id: "display"` unchanged, only change `label: "Display"` to `label: "Appearance"` (SETT-03 prep for plan 02)
    - `AdminUser` interface (lines 82-90)
    - `SystemSettings` interface (lines 92-102) and `SYS_DEFAULTS` const (lines 104-108)
    - CSS class constants: `INPUT`, `LABEL`, `HINT` (line 672-674)
    - Default `INITIAL_SETTINGS` object containing the full defaults from lines 199-215 (so each tab can use it as fallback)

    Export everything. Add `"use client"` is NOT needed here (types only + constants).

    **Step 2: Extract each tab into `components/settings/tabs/XTab.tsx`**

    Each tab component:
    - Is `"use client"`
    - Receives NO props initially (dirty-state props added in plan 02)
    - Fetches its own settings slice via `GET /api/settings` on mount
    - Has its own save function via `PUT /api/settings` with only the keys it manages
    - Owns all state variables that were previously in the monolith for that tab
    - Imports shared types/constants from `@/components/settings/types`

    Tab extraction map (source line ranges in current `app/settings/page.tsx`):

    1. **AccountTab.tsx** (lines 744-883): Account size, risk %, commission model/value, daily loss limit, Monte Carlo threshold. State: settings slice + currentBalance. Needs to fetch `/api/trades` for balance calc.

    2. **TagsTab.tsx** (lines 884-974): Default tags array, default mistakes array. State: the two arrays + save state.

    3. **TemplatesTab.tsx** (lines 975-1023): Trade templates CRUD. State: templates array + save state.

    4. **AppearanceTab.tsx** (lines 1024-1108): Heatmap ranges, charts_collapsed toggle, privacy_mode toggle. State: those 3 settings + save state.

    5. **ChartTab.tsx** (lines 1109-1268): TradingView feature toggles (6 checkboxes), default studies management. State: the TV settings + studies array + save state.

    6. **StrategiesTab.tsx** (lines 1269-1321 for JSX, but also lines 217-277 for strategy helpers and DnD setup): Move `SortableStrategy` component (lines 110-194) into this file. Move all strategy helper functions (addStrategy, removeStrategy, updateStrategyName, moveStrategy, addChecklistItem, updateChecklistItem, removeChecklistItem, handleDragEnd). Import `@dnd-kit` packages. State: strategies JSON string + expandedStrategies set + save state.

    7. **IntegrationsTab.tsx** (lines 1322-1414): Discord webhooks (chart + alert), FMP API key, Gemini API key, test buttons. State: those 4 settings + testingChart/testingAlert + test messages + save state.

    8. **BrokerTab.tsx** (lines 1415-1823): IBKR host/port/clientId, connection test, discovered accounts, account mappings, sync with date range. This is the largest tab (~409 lines). State: all `ibkr*` variables (lines 321-337). Also needs `accounts` from `useAccounts()` for the mapping dropdown. Import `useAccounts` directly.

    9. **SecurityTab.tsx** (lines 1824-1936): 2FA enable/disable flow, backup codes. State: twoFactorEnabled, tfaSetup, tfaCode, tfaLoading, tfaMsg, backupCodes, showSecret. Move the `initTfaSetup`, `enableTfa`, `disableTfa` functions (lines 610-663) into this component.

    10. **DataTab.tsx** (lines 1937-2017): Import (CSV/JSON/IBKR), export, symbol cache refresh. State: importResult, importing, importMenuOpen, refreshing, refreshMsg, symbolCount. Imports `tradesToCsv`, `csvToTrades`, `isIbkrCsv`, `ibkrCsvToTrades` from `@/lib/csv` and `TRADE_FIELDS` from `@/lib/validate-trade`.

    11. **AccountsTab.tsx** (lines 2018-2169): Multi-account CRUD. State: acctEdits, acctSaving, acctDeleting, newAcctName, acctPnls. Uses `useAccounts()` hook directly.

    12. **AdminUsersTab.tsx** (lines 2170-2251): User list, toggle admin, delete user. Receives `isAdmin: boolean` as prop (shell passes it). State: adminUsers, adminUsersLoading, adminUsersError. Also needs claiming/hasAdmin/claimMsg state for the "Claim Admin" flow.

    13. **AdminSettingsTab.tsx** (lines 2252-end): System SMTP config, app_url. Receives `isAdmin: boolean` as prop. State: sysSettings, sysLoading, sysSaving, sysSaved, showSmtpPass.

    **Important extraction rules:**
    - Each tab wraps its content in the same `<section className="rounded-xl border dark:border-slate-700 border-slate-200 dark:bg-slate-900 bg-white p-5 space-y-4">` that currently exists
    - Use `INPUT`, `LABEL`, `HINT` constants from `@/components/settings/types`
    - Each tab's save function POSTs only the keys it manages (e.g., AccountTab saves `account_size`, `risk_per_trade`, `commission_model`, `commission_value`, `daily_loss_limit`, `daily_loss_limit_type`, `montecarlo_ruin_threshold`)
    - Preserve all existing UI exactly -- same classes, same structure, same icons
    - AdminUsersTab and AdminSettingsTab receive `isAdmin` as a prop from the shell
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 13 tab components exist in components/settings/tabs/, types.ts exists in components/settings/, each file compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite settings page as thin shell</name>
  <files>app/settings/page.tsx</files>
  <action>
    Replace the 2380-line `app/settings/page.tsx` with a thin shell (~120 lines) that:

    1. **Imports:** All 13 tab components from `@/components/settings/tabs/`, types from `@/components/settings/types`
    2. **State:** Only `activeCategory` (from `useSearchParams`), `isAdmin`, `hasAdmin` (from `/api/auth/me` fetch)
    3. **Sidebar:** Keep the exact same sidebar rendering (desktop nav + mobile tabs, lines 679-734). Use `CATEGORIES` from types.ts. Keep the "Claim Admin" button for `!isAdmin && !hasAdmin`.
    4. **Content area:** Conditional rendering of each tab:
       ```tsx
       <div className="flex-1 min-w-0 space-y-6 sm:max-w-2xl min-h-[80vh]">
         <div>
           <h1>Settings</h1>
           <p>Configure your account preferences and integrations</p>
         </div>
         {active === "account" && <AccountTab />}
         {active === "tags" && <TagsTab />}
         {active === "templates" && <TemplatesTab />}
         {active === "display" && <AppearanceTab />}
         {active === "chart" && <ChartTab />}
         {active === "strategies" && <StrategiesTab />}
         {active === "integrations" && <IntegrationsTab />}
         {active === "broker" && <BrokerTab />}
         {active === "security" && <SecurityTab />}
         {active === "data" && <DataTab />}
         {active === "accounts" && <AccountsTab />}
         {active === "admin-users" && isAdmin && <AdminUsersTab isAdmin={isAdmin} />}
         {active === "admin-settings" && isAdmin && <AdminSettingsTab isAdmin={isAdmin} />}
       </div>
       ```
    5. **Remove:** The global save button (lines 2357-2368). Each tab now has its own save.
    6. **Keep:** The `Suspense` wrapper around `SettingsContent` and the `SettingsPage` export.
    7. **Keep `sm:max-w-2xl`** on the content div for now -- plan 02 removes it (SETT-01).

    The shell should NOT hold any settings state, strategy helpers, DnD imports, or tab-specific logic. Only sidebar + tab routing.
  </action>
  <verify>
    <automated>cd D:/Code/Projects/claude-test/Ledger-Of-Alpha && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>app/settings/page.tsx is under 150 lines, imports all 13 tab components, `npm run build` succeeds, visiting /settings renders the account tab by default</done>
</task>

</tasks>

<verification>
1. `npm run build` passes (TypeScript + Next.js compilation)
2. Each of the 13 tab files exists in `components/settings/tabs/`
3. `app/settings/page.tsx` is under 150 lines
4. No settings state or tab-specific logic remains in the shell page
</verification>

<success_criteria>
- The 2380-line monolith is replaced by a ~120-line shell + 13 focused tab components
- Each tab manages its own state slice and save independently
- Build passes with zero TypeScript errors
- All existing tab content is preserved identically
</success_criteria>

<output>
After completion, create `.planning/phases/13-settings-page-overhaul/13-01-SUMMARY.md`
</output>
