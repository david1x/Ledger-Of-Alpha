---
phase: 25-navbar-collapse-lock-filter-bar-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Navbar.tsx
autonomous: true
requirements:
  - NAV-LOCK
  - NAV-TOOLTIP
  - COLOR-SIDEBAR

must_haves:
  truths:
    - "Sidebar is always collapsed (icon-only) on desktop — no expand toggle exists"
    - "Hovering a nav icon shows a tooltip label to the right of the sidebar"
    - "Sidebar background is dark:bg-slate-900 bg-slate-100 — visually distinct from the page body"
    - "Account switcher tooltip appears on hover in collapsed mode"
    - "Mobile hamburger still opens the full-width sidebar with labels"
  artifacts:
    - path: "components/Navbar.tsx"
      provides: "Locked-collapsed sidebar with group-hover tooltips and cohesive bg color"
      contains: "sm:w-16"
  key_links:
    - from: "components/Navbar.tsx"
      to: "app/layout.tsx"
      via: "data-sidebar='collapsed' attribute (already set, never changes)"
      pattern: "data-sidebar"
---

<objective>
Lock the sidebar to permanently collapsed (icon-only) mode, add group-hover tooltips on all nav icons and the account switcher, and apply the cohesive background color (dark:bg-slate-900 bg-slate-100).

Purpose: The sidebar wastes horizontal space when expanded. Locking it collapsed reclaims that space and creates a cleaner app frame. Tooltips preserve discoverability. The background color change creates visual connection with the filter bar (Plan 02).
Output: Updated Navbar.tsx with no expand toggle, CSS tooltips on all icons, and new background color.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-navbar-collapse-lock-filter-bar-redesign-with-dropdown-buttons-sidebar-filter-color-cohesion/25-RESEARCH.md

@components/Navbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock sidebar collapsed and add tooltips</name>
  <files>components/Navbar.tsx</files>
  <action>
Read Navbar.tsx fully, then make these surgical changes:

**Remove expand/collapse mechanism:**
1. Remove the `expanded` state (`useState(false)` or similar)
2. Remove the `useEffect` that reads `localStorage.getItem("sidebar_expanded")` and sets the `data-sidebar` attribute
3. Remove the `useEffect` that writes `localStorage.setItem("sidebar_expanded", ...)`
4. Remove the toggle `<button>` (the floating `-right-3` button with ChevronsLeft/ChevronsRight)
5. Remove `ChevronsLeft, ChevronsRight` from the lucide-react import
6. Change `showLabels` from `expanded || mobileOpen` to just `mobileOpen`

**Lock the aside width:**
- The aside `className` should always use `sm:w-16` (never conditionally `sm:w-64`)
- Keep the mobile behavior: `mobileOpen ? "max-sm:translate-x-0 w-64" : "max-sm:-translate-x-full w-64"`

**Change sidebar background color:**
- Replace `dark:bg-slate-950 bg-white` (or current sidebar bg) with `dark:bg-slate-900 bg-slate-100`
- This creates a visible sidebar shape distinct from the page body (`dark:bg-slate-950`)

**Add group-hover tooltips on nav links:**
For each nav link in the `sidebarLinks` map:
- Add `group` class to the `<Link>` element (it should already have `relative`)
- After the icon and label, add a tooltip span when `!showLabels`:
```tsx
{!showLabels && (
  <span className="absolute left-full ml-3 top-1/2 -translate-y-1/2 px-2.5 py-1.5 text-xs font-medium rounded-md dark:bg-slate-800 bg-white dark:text-white text-slate-900 shadow-lg border dark:border-slate-700 border-slate-200 whitespace-nowrap opacity-0 group-hover:opacity-100 pointer-events-none transition-opacity z-50">
    {label}
  </span>
)}
```
- Keep the existing `title={!showLabels ? label : undefined}` for accessibility

**Add tooltip to account switcher button:**
- Apply the same `group` + tooltip span pattern to the Wallet button for the account switcher
- Tooltip text: `activeAccount?.name ?? "All Accounts"`

**Add tooltip to TradingView link (if present in sidebar):**
- Same pattern

**Add tooltip to user menu button:**
- Same pattern, using the user's name or "Account"

**Preserve completely:**
- `mobileOpen` state and the mobile hamburger toggle button
- Mobile overlay backdrop
- All dropdown logic (account switcher dropdown, user menu dropdown)
- All existing functionality — only the expand toggle is removed
  </action>
  <verify>
    <automated>cd D:/Code/Projects/claude-test/Ledger-Of-Alpha && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - No expand/collapse toggle button visible in the sidebar
    - Sidebar is always 64px (w-16) wide on desktop
    - Hovering any nav icon shows a styled tooltip to the right
    - Sidebar has dark:bg-slate-900 bg-slate-100 background
    - Mobile hamburger still works (opens full-width sidebar with labels)
    - Build passes with no type errors
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- No references to `expanded` state remain in Navbar.tsx
- No references to `localStorage` sidebar_expanded remain
- `ChevronsLeft` and `ChevronsRight` are not imported
- `group-hover:opacity-100` appears in tooltip spans
- Sidebar aside has `dark:bg-slate-900 bg-slate-100` and `sm:w-16`
</verification>

<success_criteria>
The sidebar is permanently collapsed on desktop with no toggle button. All nav icons show styled tooltips on hover. The sidebar background color is dark:bg-slate-900 bg-slate-100. Mobile sidebar still expands fully. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/25-navbar-collapse-lock-filter-bar-redesign-with-dropdown-buttons-sidebar-filter-color-cohesion/25-01-SUMMARY.md`
</output>
