---
phase: 23-sidebar-analytics-mobile-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/TradeTable.tsx
  - components/trades/TradeFilterChips.tsx
  - components/trades/SummaryStatsBar.tsx
autonomous: true
requirements:
  - MOBI-01
  - MOBI-02
  - MOBI-03

must_haves:
  truths:
    - "Trade table scrolls horizontally on small viewports with the Symbol column pinned on the left"
    - "Filter chips scroll horizontally without wrapping the layout on mobile"
    - "Summary stats bar shows 2 columns on small screens and 3 columns on sm+ screens"
  artifacts:
    - path: "components/TradeTable.tsx"
      provides: "Sticky Symbol column with solid backgrounds"
      contains: "sticky left-0"
    - path: "components/trades/TradeFilterChips.tsx"
      provides: "Horizontal scrolling chip container"
      contains: "overflow-x-auto"
    - path: "components/trades/SummaryStatsBar.tsx"
      provides: "Responsive 2-col/3-col grid"
      contains: "grid-cols-2"
  key_links:
    - from: "components/TradeTable.tsx"
      to: "overflow-x-auto wrapper"
      via: "CSS sticky within scroll container"
      pattern: "sticky left-0 z-10"
---

<objective>
Apply three targeted CSS fixes for mobile responsiveness: sticky Symbol column in the trade table, horizontally scrolling filter chips, and a 2-column stats grid on small screens.

Purpose: Make the trades page fully usable on small viewports without layout breakage.
Output: Updated TradeTable.tsx, TradeFilterChips.tsx, SummaryStatsBar.tsx with mobile-responsive CSS.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-sidebar-analytics-mobile-polish/23-CONTEXT.md
@.planning/phases/23-sidebar-analytics-mobile-polish/23-RESEARCH.md
@components/TradeTable.tsx
@components/trades/TradeFilterChips.tsx
@components/trades/SummaryStatsBar.tsx

<interfaces>
<!-- Key patterns from research -->

TradeTable.tsx structure:
- Table wrapped in `<div className="hidden sm:block overflow-x-auto p-1">` (line ~542-543)
- Symbol `<th>` at approximately line 547
- Symbol `<td>` at approximately line 606, has `font-bold text-emerald-400 cursor-pointer hover:underline`
- Rows use `<tr>` with hover classes like `hover:dark:bg-slate-800/30`
- DnD column reorder is active on headers — Symbol is in the sortable set but sticky + DnD coexist acceptably

TradeFilterChips.tsx:
- Line 62: `<div className="flex flex-wrap items-center gap-2">`
- Needs: `flex-nowrap overflow-x-auto` instead of `flex-wrap`

SummaryStatsBar.tsx:
- Line 154: `<div className="grid grid-cols-3 gap-4">`
- Needs: `grid-cols-2 sm:grid-cols-3`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sticky Symbol column in TradeTable</name>
  <files>components/TradeTable.tsx</files>
  <action>
Add CSS sticky positioning to the Symbol column header (`<th>`) and all Symbol column cells (`<td>`) in `TradeTable.tsx`.

**For the Symbol `<th>`:**
Add `sticky left-0 z-10 dark:bg-slate-900 bg-white` to the existing className. The background must be solid (not transparent) to prevent content bleed-through when scrolling horizontally.

**For the Symbol `<td>` (in each row):**
Add `sticky left-0 z-10 dark:bg-slate-900 bg-white` to the existing className. Also add hover-state background overrides to match the row hover: `group-hover:dark:bg-slate-800/30 group-hover:bg-slate-50/50` (matching whatever hover classes the `<tr>` uses).

Make sure the `<tr>` has a `group` class if it doesn't already (needed for `group-hover:` on the `<td>`).

**Important: Do NOT change the overflow-x-auto wrapper** — it must remain as-is for sticky to work within the correct scroll container. Do NOT add `overflow-hidden` to any ancestor.

**DnD note:** The Symbol column participates in DnD column reorder. The sticky classes coexist with DnD transforms — minor visual artifacts during active drag are acceptable and brief.

Find the Symbol header and cell by looking for where the "symbol" column key is rendered. The Symbol column may be identified by checking `col.key === "symbol"` or by looking for the symbol text rendering pattern (`t.symbol` or trade symbol display).
  </action>
  <verify>
    <automated>cd "D:\Code\Projects\claude-test\Ledger-Of-Alpha" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Symbol `<th>` and `<td>` elements have `sticky left-0 z-10` with solid background classes. No other table structure changed. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Filter chips horizontal scroll and stats grid responsive</name>
  <files>components/trades/TradeFilterChips.tsx, components/trades/SummaryStatsBar.tsx</files>
  <action>
**TradeFilterChips.tsx (MOBI-02):**
On line 62, change the outer container div:
- FROM: `flex flex-wrap items-center gap-2`
- TO: `flex flex-nowrap items-center gap-2 overflow-x-auto pb-1`

Add scrollbar hiding. Check if the project has a scrollbar-hide utility in `app/globals.css`. If not, add the Tailwind arbitrary property `[&::-webkit-scrollbar]:hidden` or a small inline style `style={{ scrollbarWidth: "none" }}` (for Firefox). The cleanest cross-browser approach is adding both: the Tailwind arbitrary class for WebKit AND `style={{ scrollbarWidth: "none" }}` for Firefox.

The `pb-1` prevents scrollbar from clipping chip content.

**SummaryStatsBar.tsx (MOBI-03):**
On line 154 (or wherever the grid container is), change:
- FROM: `grid grid-cols-3 gap-4`
- TO: `grid grid-cols-2 sm:grid-cols-3 gap-4`

This makes the three stat cards display in 2 columns on mobile (third card wraps below) and 3 columns on sm+ screens.
  </action>
  <verify>
    <automated>cd "D:\Code\Projects\claude-test\Ledger-Of-Alpha" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Filter chips container scrolls horizontally without wrapping on mobile. SummaryStatsBar shows 2-column grid on small screens and 3-column on sm+. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. On narrow viewport: Symbol column stays pinned left while other columns scroll right
3. On narrow viewport: filter chips scroll horizontally in a single row (no wrapping)
4. On narrow viewport: summary stats show 2 cards per row
5. Symbol column background is solid — no content bleeds through during horizontal scroll
6. Existing DnD column reorder still functions
</verification>

<success_criteria>
- Symbol column is sticky with solid backgrounds matching row hover states
- Filter chips use overflow-x-auto with hidden scrollbar
- Summary stats use grid-cols-2 sm:grid-cols-3
- No regressions in existing table, filter, or stats functionality
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/23-sidebar-analytics-mobile-polish/23-02-SUMMARY.md`
</output>
