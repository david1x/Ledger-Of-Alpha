---
phase: 24-trades-page-ui-tightening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/trades/SummaryStatsBar.tsx
  - components/trades/TradeFilterBar.tsx
  - components/trades/SavedViewsDropdown.tsx
  - components/trades/TradesSidebar.tsx
autonomous: true
requirements:
  - UI-CARDS
  - UI-BUTTONS
  - UI-SPACING

must_haves:
  truths:
    - "Summary stat cards are visually shorter than before (reduced padding and sparkline height)"
    - "All cards use rounded-lg instead of rounded-xl"
    - "All buttons across filter bar, saved views, and sidebar use rounded-md instead of rounded-lg"
    - "Stat card grid gap is tighter (gap-3 not gap-4)"
    - "Sidebar panels have tighter gaps (space-y-3 not space-y-4)"
  artifacts:
    - path: "components/trades/SummaryStatsBar.tsx"
      provides: "Compact stat cards with reduced padding and radius"
      contains: "rounded-lg"
    - path: "components/trades/TradeFilterBar.tsx"
      provides: "Filter buttons with reduced radius"
      contains: "rounded-md"
    - path: "components/trades/SavedViewsDropdown.tsx"
      provides: "Saved views buttons with reduced radius"
      contains: "rounded-md"
    - path: "components/trades/TradesSidebar.tsx"
      provides: "Sidebar panels with reduced radius and gaps"
      contains: "rounded-lg"
  key_links: []
---

<objective>
Apply spacing and border-radius polish to SummaryStatsBar, TradeFilterBar, SavedViewsDropdown, and TradesSidebar to achieve the tight grid aesthetic.

Purpose: Consistent reduced radius and tighter gaps across all trades page sub-components, matching the TradesShell changes in Plan 01.
Output: Four files with updated Tailwind classes for radius and spacing.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-trades-page-ui-tightening-wider-sidebar-compact-cards-grid-layout-polish-filter-relocation/24-CONTEXT.md
@.planning/phases/24-trades-page-ui-tightening-wider-sidebar-compact-cards-grid-layout-polish-filter-relocation/24-RESEARCH.md
@components/trades/SummaryStatsBar.tsx
@components/trades/TradeFilterBar.tsx
@components/trades/SavedViewsDropdown.tsx
@components/trades/TradesSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce SummaryStatsBar card height, radius, and gaps</name>
  <files>components/trades/SummaryStatsBar.tsx</files>
  <action>
In SummaryStatsBar.tsx, apply these Tailwind class changes:

1. CARD RADIUS: Replace all `rounded-xl` with `rounded-lg` on stat card divs.

2. CARD PADDING: Replace `p-4` with `p-3` on stat card divs to reduce card height.

3. SPARKLINE HEIGHT: Replace `h-12` with `h-10` on sparkline container divs.

4. SPARKLINE TOP PADDING: Replace `pt-2` with `pt-1` on sparkline containers.

5. GRID GAP: On the grid container that holds the 3 stat cards, change `gap-4` to `gap-3`.

These are all class-level changes. Do NOT change any logic, state, or data flow. Keep `rounded-full` on any pill/badge elements if present.
  </action>
  <verify>
    <automated>cd D:/Code/Projects/claude-test/Ledger-Of-Alpha && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Stat cards use rounded-lg, p-3, sparkline h-10 pt-1
    - Grid gap is gap-3
    - Cards appear visually shorter and tighter
  </done>
</task>

<task type="auto">
  <name>Task 2: Reduce border-radius on TradeFilterBar, SavedViewsDropdown, and TradesSidebar</name>
  <files>components/trades/TradeFilterBar.tsx, components/trades/SavedViewsDropdown.tsx, components/trades/TradesSidebar.tsx</files>
  <action>
Apply these border-radius reductions across three files:

**TradeFilterBar.tsx:**
1. Find the FILTER_BTN helper function (same pattern as TradesShell's copy). Change `rounded-lg` to `rounded-md`.
2. Find any other button or dropdown elements using `rounded-lg` and change to `rounded-md`.
3. Find any dropdown/popover menus using `rounded-lg` and change to `rounded-md`.
4. Do NOT change `rounded-full` on filter chips if any exist in this file.

**SavedViewsDropdown.tsx:**
1. Change all button `rounded-lg` to `rounded-md`.
2. Change dropdown menu container `rounded-lg` to `rounded-md`.
3. Any input fields with `rounded-lg` change to `rounded-md`.

**TradesSidebar.tsx:**
1. Change panel card containers from `rounded-xl` to `rounded-lg`.
2. Change `space-y-4` (gap between panels) to `space-y-3` on the top-level wrapper if present (this may also be set in TradesShell's sidebar div — check both places).
3. Change any buttons inside the sidebar from `rounded-lg` to `rounded-md`.
4. Do NOT change any logic, data, or component behavior.
  </action>
  <verify>
    <automated>cd D:/Code/Projects/claude-test/Ledger-Of-Alpha && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - TradeFilterBar buttons use rounded-md
    - SavedViewsDropdown buttons and menus use rounded-md
    - TradesSidebar panels use rounded-lg, internal buttons use rounded-md
    - No rounded-full elements were changed
    - All files build without errors
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- No visual regressions — only spacing/radius classes changed, no logic
- All border-radius changes follow the mapping: rounded-xl -> rounded-lg (cards), rounded-lg -> rounded-md (buttons/menus)
</verification>

<success_criteria>
All trades page sub-components have consistent reduced border-radius and tighter spacing. Cards use rounded-lg, buttons use rounded-md, gaps are reduced by one Tailwind step. The overall page has a cohesive tight grid aesthetic.
</success_criteria>

<output>
After completion, create `.planning/phases/24-trades-page-ui-tightening-wider-sidebar-compact-cards-grid-layout-polish-filter-relocation/24-02-SUMMARY.md`
</output>
