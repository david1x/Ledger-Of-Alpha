---
phase: 24-trades-page-ui-tightening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/trades/TradesShell.tsx
autonomous: true
requirements:
  - UI-LAYOUT
  - UI-SIDEBAR
  - UI-FILTER-RELOC
  - UI-COG

must_haves:
  truths:
    - "Filter bar is the first visible element on the trades page (no title above it)"
    - "New Trade button is accessible after title removal (relocated to filter row)"
    - "Sidebar border extends full height of the page content, not just sidebar content height"
    - "Sidebar is wider than before (w-80 instead of w-72)"
    - "Columns button shows a cog/gear icon with no text label"
    - "SavedViews dropdown and cog icon appear as a toolbar header inside the table card"
    - "Vertical spacing between sections is tighter (space-y-4 not space-y-6)"
    - "All buttons and cards in TradesShell use reduced border-radius"
  artifacts:
    - path: "components/trades/TradesShell.tsx"
      provides: "Restructured trades page layout"
      contains: "Settings"
  key_links:
    - from: "components/trades/TradesShell.tsx"
      to: "lucide-react"
      via: "import Settings icon"
      pattern: "import.*Settings.*from.*lucide-react"
    - from: "components/trades/TradesShell.tsx"
      to: "TradeFilterBar"
      via: "FilterBar rendered first in main column"
      pattern: "TradeFilterBar"
---

<objective>
Restructure TradesShell.tsx layout: relocate filters to top, remove title, fix sidebar full-height border, widen sidebar, replace Columns button with cog icon, embed toolbar in table card, and tighten spacing/radius.

Purpose: Transform the trades page from a title-first layout to an action-first dense grid layout per user specifications.
Output: Restructured TradesShell.tsx with new layout order, wider sidebar, full-height border, cog icon, and tighter spacing.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-trades-page-ui-tightening-wider-sidebar-compact-cards-grid-layout-polish-filter-relocation/24-CONTEXT.md
@.planning/phases/24-trades-page-ui-tightening-wider-sidebar-compact-cards-grid-layout-polish-filter-relocation/24-RESEARCH.md
@components/trades/TradesShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure layout order, remove title, relocate New Trade button and filter bar</name>
  <files>components/trades/TradesShell.tsx</files>
  <action>
In TradesShell.tsx, restructure the main content column JSX (lines 249-386) to achieve this new order:

1. REMOVE the entire header div (lines 250-268) containing "Trade Log" h1, trade count p, and New Trade button wrapper. The title and trade count are deleted permanently.

2. Move TradeFilterBar to be the FIRST element in the main column. Add the New Trade button and TradeImportExport to the RIGHT side of the filter bar row. Wrap them in a flex container:
   ```tsx
   <div className="flex items-start gap-3">
     <div className="flex-1 min-w-0">
       <TradeFilterBar ... />
     </div>
     <div className="flex items-center gap-1.5 sm:gap-2 shrink-0 pt-0.5">
       <TradeImportExport activeAccountId={activeAccountId} onTradesChanged={load} />
       <button onClick={...} className="flex items-center gap-1.5 h-8 sm:h-9 px-3 sm:px-4 rounded-md bg-emerald-600 hover:bg-emerald-500 text-white text-[10px] sm:text-sm font-bold transition-all shadow-lg shadow-emerald-600/10 active:scale-95">
         <Plus className="w-3.5 h-3.5 sm:w-4 sm:h-4" />
         <span className="hidden xs:inline">New Trade</span>
         <span className="xs:hidden">New</span>
       </button>
     </div>
   </div>
   ```

3. TradeFilterChips stays immediately after the filter bar row (position 2).

4. SummaryStatsBar moves AFTER filter chips (position 3).

5. Mobile tab toggle stays at position 4.

6. For the table section (position 5), embed the SavedViews + cog toolbar as a header INSIDE the table card (Option B from research):
   ```tsx
   <div className="rounded-lg dark:bg-slate-800/50 bg-white">
     <div className="flex items-center justify-end gap-2 px-4 pt-3 pb-2 border-b dark:border-slate-700/50 border-slate-100">
       <SavedViewsDropdown ... />
       <div className="relative" ref={columnMenuRef}>
         <button onClick={...} className={FILTER_BTN(showColumnMenu)} title="Configure columns">
           <Settings className="w-4 h-4" />
         </button>
         {showColumnMenu && (column dropdown menu unchanged, but reduce its rounded-lg to rounded-md)}
       </div>
     </div>
     <div className="px-4 pb-4 pt-2">
       <TradeTable ... />
     </div>
   </div>
   ```
   Remove the standalone SavedViews + Columns row div (lines 288-332) since it is now embedded in the table card.

7. Mobile analytics tab content stays at position 6.

Also update the New Trade button's rounded-lg to rounded-md (matching button radius reduction).
  </action>
  <verify>
    <automated>cd D:/Code/Projects/claude-test/Ledger-Of-Alpha && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - "Trade Log" title and trade count are removed from the page
    - TradeFilterBar is the first element in the main column
    - New Trade button and import/export sit to the right of the filter bar
    - TradeFilterChips follows the filter bar
    - SummaryStatsBar follows the filter chips
    - SavedViews + cog icon are inside the table card as a toolbar header with border-b separator
    - Column dropdown menu trigger shows only a Settings (cog) icon, no "Columns" text
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix sidebar full-height border, widen sidebar, tighten spacing and radius</name>
  <files>components/trades/TradesShell.tsx</files>
  <action>
In TradesShell.tsx, apply these changes:

1. SIDEBAR FULL-HEIGHT BORDER: Change the outer flex wrapper (line 247):
   - From: `className="flex gap-0 items-start"`
   - To: `className="flex gap-0 items-stretch"`
   The aside's border-l will now extend to match the tallest sibling (the main column).

2. SIDEBAR WIDTH: Change the open sidebar inner div (line 391):
   - From: `w-72` (288px)
   - To: `w-80` (320px)

3. SIDEBAR PANEL GAPS: Change `space-y-4` to `space-y-3` on the sidebar inner div (line 391).

4. MAIN COLUMN SPACING: Change the main column div:
   - From: `space-y-6`
   - To: `space-y-4`

5. FILTER_BTN RADIUS: Update the FILTER_BTN helper (line 238):
   - From: `rounded-lg`
   - To: `rounded-md`

6. ICON IMPORT: Update the import statement (line 6):
   - From: `import { Plus, SlidersHorizontal, PanelRightClose, PanelRightOpen } from "lucide-react";`
   - To: `import { Plus, Settings, PanelRightClose, PanelRightOpen } from "lucide-react";`
   Remove `SlidersHorizontal`, add `Settings`.

7. SIDEBAR TOGGLE BUTTON RADIUS: Change the collapse button's `rounded-lg` to `rounded-md` (line 396).

8. TABLE CONTAINER RADIUS: Already handled in Task 1 (changed from rounded-xl to rounded-lg).

9. COLUMN DROPDOWN RADIUS: The dropdown div's `rounded-lg` changes to `rounded-md` (already inside table card from Task 1).
  </action>
  <verify>
    <automated>cd D:/Code/Projects/claude-test/Ledger-Of-Alpha && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Outer flex uses items-stretch so sidebar border extends full page height
    - Sidebar width is w-80 (320px) when open
    - Sidebar panel gaps reduced to space-y-3
    - Main column uses space-y-4 (not space-y-6)
    - FILTER_BTN uses rounded-md
    - Settings icon imported instead of SlidersHorizontal
    - All buttons in TradesShell use reduced border-radius
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- No TypeScript errors (SlidersHorizontal removed, Settings imported)
- Visual inspection: trades page loads with filters at top, no title, cog icon, wider sidebar with full-height border
</verification>

<success_criteria>
TradesShell.tsx is restructured with: filters at top, no title, New Trade button in filter row, toolbar inside table card with cog icon, wider sidebar (w-80), full-height border (items-stretch), tighter spacing (space-y-4), reduced border-radius on all buttons/cards.
</success_criteria>

<output>
After completion, create `.planning/phases/24-trades-page-ui-tightening-wider-sidebar-compact-cards-grid-layout-polish-filter-relocation/24-01-SUMMARY.md`
</output>
