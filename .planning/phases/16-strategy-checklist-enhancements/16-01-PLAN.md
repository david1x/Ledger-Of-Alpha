---
phase: 16-strategy-checklist-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/strategies.ts
  - lib/types.ts
  - lib/db.ts
  - app/api/trades/route.ts
  - app/api/trades/[id]/route.ts
  - components/TradeModal.tsx
  - components/settings/tabs/StrategiesTab.tsx
autonomous: true
requirements:
  - STRAT-01
  - STRAT-02
  - STRAT-03
  - STRAT-05

must_haves:
  truths:
    - "Selecting a strategy snapshots its checklist into the trade's checklist_state column"
    - "User can add, remove, and edit individual checklist items on a trade without affecting the strategy template"
    - "User can add ad-hoc checklist items when no strategy is selected"
    - "A fresh install with no strategies setting seeds 5 built-in strategies"
    - "Switching strategies on a trade with checked items shows a confirmation dialog"
    - "Default strategy definitions live in exactly one file: lib/strategies.ts"
  artifacts:
    - path: "lib/strategies.ts"
      provides: "5 built-in default strategies with max 5 items each"
      exports: ["DEFAULT_STRATEGIES"]
    - path: "lib/types.ts"
      provides: "ChecklistItem type and checklist_state field on Trade"
      contains: "ChecklistItem"
    - path: "lib/db.ts"
      provides: "Migration 022 adding checklist_state column"
      contains: "022_checklist_state"
    - path: "components/TradeModal.tsx"
      provides: "Overhauled StrategyChecklist with inline editing, ad-hoc input, copy-on-select"
      min_lines: 100
  key_links:
    - from: "components/TradeModal.tsx"
      to: "lib/strategies.ts"
      via: "import DEFAULT_STRATEGIES"
      pattern: "import.*DEFAULT_STRATEGIES.*from.*lib/strategies"
    - from: "components/TradeModal.tsx"
      to: "/api/trades"
      via: "POST/PUT with checklist_state JSON"
      pattern: "checklist_state"
    - from: "app/api/trades/route.ts"
      to: "trades table"
      via: "INSERT with checklist_state column"
      pattern: "checklist_state"
    - from: "components/settings/tabs/StrategiesTab.tsx"
      to: "lib/strategies.ts"
      via: "import DEFAULT_STRATEGIES for seeding"
      pattern: "import.*DEFAULT_STRATEGIES.*from.*lib/strategies"
---

<objective>
Consolidate default strategies to lib/strategies.ts, add the checklist_state DB column, and overhaul the TradeModal StrategyChecklist component to support per-trade checklist editing, ad-hoc items, and copy-on-select behavior.

Purpose: This is the core data model and UI change that enables traders to customize checklists per-trade without mutating strategy templates, and always have useful default strategies available.
Output: lib/strategies.ts, updated Trade type, migration 022, overhauled StrategyChecklist component, updated API routes.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-strategy-checklist-enhancements/16-CONTEXT.md

@lib/types.ts
@lib/db.ts
@components/TradeModal.tsx
@components/settings/tabs/StrategiesTab.tsx
@app/api/trades/route.ts
@app/api/trades/[id]/route.ts

<interfaces>
<!-- Key types the executor needs -->

From lib/types.ts:
```typescript
export interface Trade {
  // ... existing fields ...
  strategy_id?: string | null;
  checklist_items?: string | null;  // legacy comma-separated
  // checklist_state will be added
}

export interface TradeStrategy {
  id: string;
  name: string;
  checklist: string[];
}
```

From components/TradeModal.tsx (current DEFAULT_STRATEGIES to be moved):
```typescript
const DEFAULT_STRATEGIES: TradeStrategy[] = [
  { id: "wyckoff_buy", name: "Wyckoff Buying Tests", checklist: [...9 items...] },
  { id: "wyckoff_sell", name: "Wyckoff Selling Tests", checklist: [...9 items...] }
];
```

Current StrategyChecklist (line 537): reads checklist as comma-separated string, toggles items by text matching. This entire component needs to be rewritten to use ChecklistItem[] state.

Current checklist_items column: stores comma-separated checked item TEXT (e.g., "Item A, Item B"). Migration must handle backward compat.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/strategies.ts, add types, DB migration, and update API routes</name>
  <files>lib/strategies.ts, lib/types.ts, lib/db.ts, app/api/trades/route.ts, app/api/trades/[id]/route.ts, components/settings/tabs/StrategiesTab.ts</files>
  <action>
1. **Create `lib/strategies.ts`** with 5 default strategies (each max 5 items):
   - `{ id: "wyckoff", name: "Wyckoff", checklist: [...] }` — unified buy/sell (per user decision: single strategy, not split)
   - `{ id: "smc", name: "SMC", checklist: [...] }` — Smart Money Concepts
   - `{ id: "breakout", name: "Breakout", checklist: [...] }`
   - `{ id: "reversal", name: "Reversal", checklist: [...] }`
   - `{ id: "sma150", name: "150 SMA", checklist: [...] }`
   Export as `export const DEFAULT_STRATEGIES: TradeStrategy[]`. Import `TradeStrategy` from `lib/types.ts`.

2. **Update `lib/types.ts`**:
   - Add `ChecklistItem` interface: `{ text: string; checked: boolean }`
   - Add `checklist_state?: string | null` to the `Trade` interface (JSON string of ChecklistItem[])

3. **Add migration 022 in `lib/db.ts`**:
   - Migration name: `022_checklist_state`
   - Add `checklist_state TEXT` column to trades table
   - Backfill existing rows: for each trade that has `checklist_items` (comma-separated checked text) AND `strategy_id`, parse: get strategy checklist from strategies setting or DEFAULT_STRATEGIES, build ChecklistItem[] where each item has `checked: true` if its text appears in `checklist_items`. Write JSON to `checklist_state`.
   - Note: `checklist_items` stores comma-separated TEXT of CHECKED items only. The strategy template has the full list. So backfill must: (a) get strategy checklist items as the full list, (b) mark each as checked if it appears in `checklist_items` text.

4. **Update `app/api/trades/route.ts`** (POST):
   - Add `checklist_state` to the destructured body fields
   - Add `checklist_state` to the INSERT column list and VALUES placeholders
   - Pass `checklist_state ?? null` in the parameter array

5. **Update `app/api/trades/[id]/route.ts`** (PUT):
   - Add `checklist_state = ?` to the SET clause
   - Pass `merged.checklist_state ?? null` in the parameter array

6. **Update `components/settings/tabs/StrategiesTab.tsx`**:
   - Replace the local `DEFAULT_STRATEGIES` (currently the 2 Wyckoff strategies hardcoded inline) with `import { DEFAULT_STRATEGIES } from "@/lib/strategies"`
   - The seeding logic (`strategies` setting empty -> use defaults) already exists; just make sure it imports from the new shared source
  </action>
  <verify>
    <automated>cd D:/Code/Projects/claude-test/Ledger-Of-Alpha && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>lib/strategies.ts exists with 5 strategies, Trade type has checklist_state, migration 022 exists, API routes handle checklist_state, StrategiesTab imports from shared source. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Overhaul StrategyChecklist for per-trade editing, ad-hoc items, and copy-on-select</name>
  <files>components/TradeModal.tsx</files>
  <action>
1. **Remove `DEFAULT_STRATEGIES` constant** from TradeModal.tsx. Import from `@/lib/strategies` instead.

2. **Add `ChecklistItem` import** from `@/lib/types`.

3. **Replace form's checklist data model**: Instead of `form.checklist_items` (comma string), use a local `checklistState` React state of type `ChecklistItem[]`. Initialize from:
   - If editing existing trade: parse `trade.checklist_state` JSON if present, else backfill from `trade.checklist_items` + strategy (same logic as migration)
   - If new trade: empty array until strategy selected

4. **Rewrite `StrategyChecklist` component** (currently at line 537). New behavior:
   - **Strategy dropdown**: Add "No Strategy" as the first `<option value="">No Strategy</option>`. Current strategies follow.
   - **Copy-on-select**: When a strategy is selected, snapshot its checklist items into `checklistState` as `ChecklistItem[]` (all unchecked). If switching from a strategy with existing checked items, show `AlertModal` confirmation first (import AlertModal from `@/components/AlertModal`). If "No Strategy" selected, clear template items but keep any ad-hoc items (or clear all — user decision says "clears template items").
   - **Checklist rendering**: Render from `checklistState` array (not from strategy template). Each item shows:
     - Checkbox (toggle `checked`)
     - Text label (editable: click an edit icon to inline-edit the text)
     - Remove button (X icon, removes item from array)
   - **Inline editing**: Small pencil/edit icon on each item. Clicking it turns the text into an input field. Enter/blur commits the edit. This only changes `checklistState`, never the strategy template.
   - **Ad-hoc input**: Below the checklist grid, add a free-text input with an "Add" button. Typing text and clicking Add (or pressing Enter) appends a new `{ text: inputValue, checked: false }` to `checklistState`. This input appears ALWAYS (whether a strategy is selected or not), per user decision.
   - **Score display**: Keep the existing `X / Y` counter badge but compute from `checklistState` (checked count / total count).

5. **Wire checklistState to form submission**: When saving the trade, serialize `checklistState` as JSON string into `form.checklist_state`. Also maintain `form.checklist_items` as comma-separated checked item texts for backward compatibility with any code that reads it.

6. **Update the `set` helper calls**: Replace `onChecklistChange` / `onStrategyChange` callbacks. The StrategyChecklist component should receive `checklistState`, `setChecklistState`, `strategyId`, `onStrategyChange`, and `strategies` as typed props (not `any`).

7. **Props cleanup**: Type the StrategyChecklist props properly instead of using `: any`.
  </action>
  <verify>
    <automated>cd D:/Code/Projects/claude-test/Ledger-Of-Alpha && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Selecting a strategy copies its items into checklistState (copy-on-select)
    - "No Strategy" option exists in dropdown and allows ad-hoc-only checklists
    - Each checklist item can be toggled, edited inline, or removed
    - Free-text Add input appears below checklist for ad-hoc items
    - Switching strategies with checked items triggers AlertModal confirmation
    - checklist_state JSON is saved with the trade
    - Build succeeds with no errors
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no TypeScript errors
2. Open TradeModal on an existing trade — checklist_state loads correctly
3. Select a strategy — items appear unchecked (copy-on-select)
4. Add ad-hoc item via free-text input — appears in list
5. Edit an item inline — change persists in local state
6. Remove an item — disappears from list
7. Switch strategy with checked items — confirmation dialog appears
8. Select "No Strategy" — template items clear, ad-hoc input available
9. Save trade — checklist_state column populated with JSON
10. Settings > Strategies tab still works, imports from lib/strategies.ts
</verification>

<success_criteria>
- lib/strategies.ts is the single source of truth for 5 default strategies (STRAT-05)
- Per-trade checklist editing works without mutating strategy templates (STRAT-01)
- Ad-hoc items work with or without a strategy selected (STRAT-02)
- Fresh install seeds 5 built-in strategies (STRAT-03)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/16-strategy-checklist-enhancements/16-01-SUMMARY.md`
</output>
