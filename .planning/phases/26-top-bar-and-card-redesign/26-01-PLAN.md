---
phase: 26-top-bar-and-card-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/DashboardShell.tsx
autonomous: false
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
  - LAYOUT-04

must_haves:
  truths:
    - "Dashboard has a fixed h-16 top bar with account balance, P&L, today P&L, trades count, and win rate inline"
    - "Time filter pills, edit mode toggle, reset, templates, refresh, export, privacy toggle, and New Trade button are all in the top bar"
    - "Page title 'Dashboard' and subtitle 'Your trading performance at a glance' are gone"
    - "Dashboard content scrolls independently below the top bar with no page-level scrollbar"
  artifacts:
    - path: "components/dashboard/DashboardShell.tsx"
      provides: "Viewport-locked dashboard shell with top bar"
      contains: "h-16 shrink-0 border-b"
  key_links:
    - from: "DashboardShell.tsx root div"
      to: "flex flex-col overflow-hidden"
      via: "viewport-locked flex shell"
      pattern: "flex flex-col.*overflow-hidden"
    - from: "top bar"
      to: "account stats + layout controls"
      via: "h-16 flex bar with left stats, right controls"
      pattern: "h-16 shrink-0 border-b"
    - from: "content area"
      to: "weekly calendar + widget grid + hidden panel + open trades"
      via: "flex-1 min-h-0 overflow-y-auto scrollable container"
      pattern: "flex-1 min-h-0 overflow-y-auto"
---

<objective>
Restructure DashboardShell.tsx to use a viewport-locked flex layout with a navbar-style h-16 top bar containing inline account stats and all layout controls.

Purpose: Match the trades page design language — same top bar pattern, same viewport locking, same density.
Output: DashboardShell with top bar and viewport-locked content area. No page title, no separate account summary strip.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-top-bar-and-card-redesign/26-CONTEXT.md
@.planning/phases/26-top-bar-and-card-redesign/26-RESEARCH.md

@components/dashboard/DashboardShell.tsx
@components/trades/TradesShell.tsx

<interfaces>
<!-- TradesShell viewport-lock pattern (reference implementation) -->
From components/trades/TradesShell.tsx:
```tsx
// Line 243-245 — the escape + flex shell pattern
<div className="flex flex-col -mx-6 -mt-6 -mb-6 overflow-hidden" style={{ height: "100vh" }}>
  <div className="px-6 flex items-center h-16 shrink-0 border-b dark:border-slate-800 border-slate-200 dark:bg-slate-900 bg-slate-100">
```

<!-- Current DashboardShell structure (lines to change) -->
From components/dashboard/DashboardShell.tsx:
```tsx
// Line 1068 — current root (REPLACE)
<div className="space-y-6">

// Lines 1070-1073 — title/subtitle (DELETE)
<h1 className="text-2xl ...">Dashboard</h1>
<p className="text-sm ...">Your trading performance at a glance</p>

// Lines 1076-1163 — controls block (RELOCATE into top bar right side)
// Lines 1166-1218 — account summary strip (ABSORB into top bar left side, DELETE outer card wrapper)
```

<!-- Existing variables available in DashboardShell render scope -->
- `currentBalance`, `totalPnl`, `todayPnl`, `accountSize` — computed numbers
- `closed` — array of closed trades (for trades count, win rate)
- `hidden`, `mask`, `toggleHidden` — privacy mode
- `fmt$()` — currency formatter
- `timeFilter`, `saveTimeFilter`, `timeFilters` — time filter state
- `editMode`, `setEditMode` — edit mode toggle
- `showExportMenu`, `setShowExportMenu` — export dropdown
- `loading`, `load` — refresh state
- `dailyLossExceeded`, `dailyLossLimit`, `dailyLossLimitType` — daily loss warning
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace root container with viewport-locked flex shell and build top bar</name>
  <files>components/dashboard/DashboardShell.tsx</files>
  <action>
In DashboardShell.tsx, restructure the return JSX (starting at line 1067):

1. **Replace root div** (line 1068): Change `<div className="space-y-6">` to `<div className="flex flex-col -mx-6 -mt-6 -mb-6 overflow-hidden" style={{ height: "100vh" }}>` — this escapes layout padding and creates the viewport-locked shell (same pattern as TradesShell.tsx line 243).

2. **Delete title and subtitle** (lines 1072-1073): Remove the `<h1>Dashboard</h1>` and `<p>Your trading performance at a glance</p>` entirely, along with their wrapper div.

3. **Build the top bar** as the first child of the root div:
```tsx
<div className="px-6 flex items-center h-16 shrink-0 border-b dark:border-slate-800 border-slate-200 dark:bg-slate-900 bg-slate-100">
  {/* LEFT: Account stats */}
  <div className="flex items-center gap-4 flex-1 min-w-0 overflow-hidden">
    {/* Balance */}
    <div className="flex items-center gap-2">
      <span className="text-xs font-medium dark:text-slate-400 text-slate-500 uppercase tracking-wider">Balance</span>
      <span className={clsx("text-sm font-bold", totalPnl >= 0 ? "text-emerald-400" : "text-red-400")}>
        {hidden ? mask : `$${currentBalance.toFixed(2)}`}
      </span>
    </div>
    <div className="hidden sm:block w-px h-5 dark:bg-slate-700 bg-slate-200" />
    {/* P&L */}
    <div className="flex items-center gap-2">
      <span className="text-xs font-medium dark:text-slate-400 text-slate-500 uppercase tracking-wider">P&L</span>
      <span className={clsx("text-sm font-bold", totalPnl >= 0 ? "text-emerald-400" : "text-red-400")}>
        {hidden ? mask : `${totalPnl >= 0 ? "+" : "-"}${fmt$(totalPnl)}`}
      </span>
      <span className={clsx("hidden xl:inline text-xs font-semibold", totalPnl >= 0 ? "text-emerald-400/70" : "text-red-400/70")}>
        {hidden ? "" : `(${totalPnl >= 0 ? "+" : ""}${(totalPnl / accountSize * 100).toFixed(2)}%)`}
      </span>
    </div>
    <div className="hidden lg:block w-px h-5 dark:bg-slate-700 bg-slate-200" />
    {/* Today */}
    <div className="hidden lg:flex items-center gap-2">
      <span className="text-xs font-medium dark:text-slate-400 text-slate-500 uppercase tracking-wider">Today</span>
      <span className={clsx("text-sm font-bold", todayPnl >= 0 ? "text-emerald-400" : "text-red-400")}>
        {hidden ? mask : `${todayPnl >= 0 ? "+" : "-"}${fmt$(todayPnl)}`}
      </span>
    </div>
    <div className="hidden lg:block w-px h-5 dark:bg-slate-700 bg-slate-200" />
    {/* Trades count */}
    <div className="hidden xl:flex items-center gap-2">
      <span className="text-xs font-medium dark:text-slate-400 text-slate-500 uppercase tracking-wider">Trades</span>
      <span className="text-sm font-bold dark:text-slate-300 text-slate-700">{closed.length}</span>
    </div>
    <div className="hidden xl:block w-px h-5 dark:bg-slate-700 bg-slate-200" />
    {/* Win Rate */}
    <div className="hidden sm:flex items-center gap-2">
      <span className="text-xs font-medium dark:text-slate-400 text-slate-500 uppercase tracking-wider">Win Rate</span>
      {/* compute wins/wr inline same as current lines 1209-1214 */}
    </div>
  </div>

  {/* RIGHT: Controls */}
  <div className="flex items-center gap-2 shrink-0 ml-4">
    {/* Time filter pills — MOVE from current lines 1078-1092, keep existing styles */}
    {/* Utility button group — MOVE from current lines 1096-1155, keep existing styles */}
    {/* New Trade button — MOVE from current lines 1157-1161 */}
    {/* Adjust New Trade button: change rounded-2xl to rounded-md, h-10 to h-9, px-5 to px-4 */}
  </div>
</div>
```

Responsive stat visibility strategy (per research recommendation):
- Balance: always visible
- P&L value: always visible; percentage suffix hidden below xl (`hidden xl:inline`)
- Win Rate: hidden below sm (`hidden sm:flex`)
- Today P&L: hidden below lg (`hidden lg:flex`)
- Trades count: hidden below xl (`hidden xl:flex`)

4. **Delete the account summary strip** (lines 1166-1218): Remove the entire `<div className="rounded-2xl dark:bg-slate-900/80 ...">` block — its content is now in the top bar.

5. **Wrap remaining content in scrollable area**: Everything after the top bar (daily loss warning, weekly calendar, widget grid, hidden widgets panel, open trades, modals) goes inside:
```tsx
<div className="flex-1 min-h-0 overflow-y-auto px-6 py-4 space-y-4">
  {/* dailyLossExceeded warning */}
  {/* WeeklyCalendar */}
  {/* Widget grid (DndContext) */}
  {/* Hidden widgets panel */}
  {/* Open Trades */}
</div>
```

Note: The `TradeModal` and `AlertModal` should remain OUTSIDE the scrollable div (they are portals/overlays and should be siblings of the scrollable area, inside the root flex container).

6. **Adjust New Trade button**: Change `h-10 px-5 rounded-2xl` to `h-9 px-4 rounded-md` to fit the h-16 bar (per research recommendation).

Keep all existing state variables, event handlers, and logic unchanged. This is a structural JSX reorganization only.
  </action>
  <verify>
    <automated>cd D:/Code/Projects/claude-test/Ledger-Of-Alpha && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Root div uses `flex flex-col -mx-6 -mt-6 -mb-6 overflow-hidden` with `height: 100vh`
    - h-16 top bar with `dark:bg-slate-900 bg-slate-100 border-b` contains account stats left and controls right
    - Page title and subtitle are gone
    - Account summary strip card is gone (absorbed into top bar)
    - Content area uses `flex-1 min-h-0 overflow-y-auto` and contains weekly calendar, widget grid, hidden panel, open trades
    - No page-level scrollbar — only content area scrolls
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify top bar layout and viewport locking</name>
  <files>components/dashboard/DashboardShell.tsx</files>
  <action>Human verifies the top bar and viewport-locked layout visually.</action>
  <what-built>Dashboard top bar with inline account stats and viewport-locked layout matching the trades page design language</what-built>
  <how-to-verify>
    1. Visit http://localhost:3000 (dashboard)
    2. Verify the top bar is visible with account stats (Balance, P&L, Win Rate) on the left and controls (time filter, edit mode, refresh, export, privacy, New Trade) on the right
    3. Verify the page title "Dashboard" and subtitle are gone
    4. Verify the separate account summary strip card is gone
    5. Scroll the widget grid — verify only the content area scrolls, not the whole page (top bar stays fixed)
    6. Resize the browser window narrower — verify stats hide progressively (Today and Trades disappear at smaller widths)
    7. Click Edit mode — verify template panel, reset button appear in the utility group
    8. Toggle privacy — verify all stats in the top bar get masked
  </how-to-verify>
  <verify>Visual inspection by user</verify>
  <done>User confirms top bar layout and viewport locking work correctly</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes without type errors
- Dashboard loads at localhost:3000 with new top bar layout
- No page-level scrollbar visible
- All controls functional (time filter, edit mode, privacy, refresh, export, New Trade)
</verification>

<success_criteria>
- Dashboard has h-16 top bar with inline account stats and all layout controls
- Page title and subtitle removed
- Viewport-locked layout with independent content scrolling
- All existing functionality preserved (edit mode, drag reorder, time filtering, privacy, templates)
</success_criteria>

<output>
After completion, create `.planning/phases/26-top-bar-and-card-redesign/26-01-SUMMARY.md`
</output>
