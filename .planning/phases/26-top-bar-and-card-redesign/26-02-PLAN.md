---
phase: 26-top-bar-and-card-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - components/dashboard/DashboardShell.tsx
  - components/dashboard/WeeklyCalendar.tsx
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - CARD-03

must_haves:
  truths:
    - "All widget cards use rounded-md borders with no shadow-sm"
    - "Card borders match trades page style (dark:border-slate-800 border-slate-200)"
    - "No recent trades table widget exists on the dashboard"
    - "All card-like elements (hidden widgets panel, open trades, daily loss warning, weekly calendar, empty state, export dropdown) use consistent rounded-md border styling"
  artifacts:
    - path: "components/dashboard/DashboardShell.tsx"
      provides: "Unified card styling via WidgetCard and all standalone card-like elements"
      contains: "rounded-md border dark:border-slate-800"
    - path: "components/dashboard/WeeklyCalendar.tsx"
      provides: "Updated calendar strip with rounded-md border"
      contains: "rounded-md"
  key_links:
    - from: "WidgetCard className"
      to: "all rendered widgets"
      via: "single wrapper component"
      pattern: "rounded-md border.*dark:border-slate-800"
    - from: "WeeklyCalendar outer div"
      to: "calendar strip rendering"
      via: "className on container ref div"
      pattern: "rounded-md.*border"
---

<objective>
Update all card-like elements on the dashboard to use unified rounded-md border styling matching the trades page design language.

Purpose: Visual consistency between dashboard and trades page. Cards use borders for definition, not shadows.
Output: All dashboard cards, panels, and card-like containers use `rounded-md border dark:border-slate-800 border-slate-200` with no `shadow-sm`.
</objective>

<execution_context>
@C:/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-top-bar-and-card-redesign/26-CONTEXT.md
@.planning/phases/26-top-bar-and-card-redesign/26-RESEARCH.md
@.planning/phases/26-top-bar-and-card-redesign/26-01-SUMMARY.md

@components/dashboard/DashboardShell.tsx
@components/dashboard/WeeklyCalendar.tsx

<interfaces>
<!-- Elements that need rounded-md treatment in DashboardShell.tsx -->

1. WidgetCard (line 246):
   BEFORE: `rounded-2xl dark:bg-slate-800/50 bg-white p-3 flex flex-col shadow-sm ${spanClass}`
   AFTER:  `rounded-md border dark:border-slate-800 border-slate-200 dark:bg-slate-800/50 bg-white p-3 flex flex-col ${spanClass}`

2. Empty state div (line 1243):
   BEFORE: `rounded-2xl border dark:border-slate-700 border-slate-200 dark:bg-slate-800/50 bg-slate-50 p-8 text-center shadow-sm`
   AFTER:  `rounded-md border dark:border-slate-700 border-slate-200 dark:bg-slate-800/50 bg-slate-50 p-8 text-center`

3. Hidden widgets panel (line 1270):
   BEFORE: `rounded-2xl border dark:border-slate-700 border-slate-200 dark:bg-slate-800/30 bg-slate-50 p-3 shadow-sm`
   AFTER:  `rounded-md border dark:border-slate-700 border-slate-200 dark:bg-slate-800/30 bg-slate-50 p-3`

4. Hidden widget restore buttons (line 1275):
   BEFORE: `rounded-xl border ...`
   AFTER:  `rounded-md border ...`

5. Open trades wrapper (line 1292):
   BEFORE: `rounded-2xl dark:bg-slate-800/50 bg-white p-4 shadow-sm border dark:border-slate-800/50 border-slate-100/50`
   AFTER:  `rounded-md dark:bg-slate-800/50 bg-white p-4 border dark:border-slate-800 border-slate-200`

6. Daily loss warning (line 1222):
   BEFORE: `rounded-2xl border border-red-500/30 bg-red-500/10 p-4 ... shadow-sm`
   AFTER:  `rounded-md border border-red-500/30 bg-red-500/10 p-4 ...` (remove shadow-sm)

7. Export dropdown (line 1141):
   BEFORE: `rounded-2xl border dark:border-slate-700 border-slate-200 dark:bg-slate-900 bg-white shadow-2xl ...`
   AFTER:  `rounded-md border dark:border-slate-700 border-slate-200 dark:bg-slate-900 bg-white shadow-2xl ...` (keep shadow-2xl on dropdown — it's a floating overlay, not a card)

<!-- WeeklyCalendar.tsx -->
8. Container ref div (line 109):
   BEFORE: `rounded-xl dark:bg-slate-900/80 bg-white px-4 py-3`
   AFTER:  `rounded-md border dark:border-slate-800 border-slate-200 dark:bg-slate-900/80 bg-white px-4 py-3`

9. Day popup (line 174):
   BEFORE: `rounded-xl border ...`
   AFTER:  `rounded-md border ...`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update WidgetCard and all DashboardShell card-like elements to rounded-md border styling</name>
  <files>components/dashboard/DashboardShell.tsx</files>
  <action>
Perform a systematic sweep of DashboardShell.tsx, updating every card-like element to use unified styling. Use grep to find ALL occurrences of `rounded-2xl` and `shadow-sm` in the file first, then update each one.

**WidgetCard component (line 246):**
Change className from:
`rounded-2xl dark:bg-slate-800/50 bg-white p-3 flex flex-col shadow-sm ${spanClass}`
to:
`rounded-md border dark:border-slate-800 border-slate-200 dark:bg-slate-800/50 bg-white p-3 flex flex-col ${spanClass}`

**Empty state div (line ~1243):**
Change `rounded-2xl` to `rounded-md` and remove `shadow-sm`.

**Hidden widgets panel (line ~1270):**
Change `rounded-2xl` to `rounded-md` and remove `shadow-sm`.

**Hidden widget restore buttons (line ~1275):**
Change `rounded-xl` to `rounded-md` and remove `shadow-sm`.

**Open trades wrapper (line ~1292):**
Change `rounded-2xl` to `rounded-md`, remove `shadow-sm`, update border to full opacity: `border dark:border-slate-800 border-slate-200`.

**Daily loss warning (line ~1222):**
Change `rounded-2xl` to `rounded-md`, remove `shadow-sm`.

**Export dropdown (line ~1141):**
Change `rounded-2xl` to `rounded-md`. Keep `shadow-2xl` (it's a floating overlay that needs shadow for depth).

After changes, grep the file for any remaining `rounded-2xl` or `rounded-xl` (excluding the time filter pills and utility group which are UI controls, not cards) and for any remaining `shadow-sm` to confirm no card-like elements were missed.

**CARD-02 verification:** Confirm no widget with id "recent-trades" exists in ALL_WIDGETS array or DEFAULT_ORDER. The requirement is already satisfied — no code change needed, just verify.

**CARD-03 verification:** WidgetCard is already the single wrapper for all widgets (every widget in the grid goes through WidgetCard). The className update in this task ensures all widgets share the unified style through this single component.
  </action>
  <verify>
    <automated>cd D:/Code/Projects/claude-test/Ledger-Of-Alpha && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - WidgetCard uses `rounded-md border dark:border-slate-800 border-slate-200` with no `shadow-sm`
    - Empty state, hidden widgets panel, open trades wrapper, daily loss warning all use `rounded-md` with no `shadow-sm`
    - Export dropdown uses `rounded-md` (keeps shadow-2xl for overlay depth)
    - Hidden widget restore buttons use `rounded-md`
    - No `rounded-2xl` remains on any card-like element (time filter pills and utility group are exempt as UI controls)
    - No "recent-trades" widget exists in ALL_WIDGETS
  </done>
</task>

<task type="auto">
  <name>Task 2: Update WeeklyCalendar styling to match new card conventions</name>
  <files>components/dashboard/WeeklyCalendar.tsx</files>
  <action>
Update WeeklyCalendar.tsx card-like elements:

1. **Outer container** (line 109): Change `rounded-xl dark:bg-slate-900/80 bg-white px-4 py-3` to `rounded-md border dark:border-slate-800 border-slate-200 dark:bg-slate-900/80 bg-white px-4 py-3`.

2. **Day popup** (line 174): Change `rounded-xl` to `rounded-md` in the popup className.

3. **Day cells** (line 136): Change `rounded-xl` to `rounded-md` in the day cell className.

Do NOT change small UI elements like internal buttons (`rounded` for hover states is fine).
  </action>
  <verify>
    <automated>cd D:/Code/Projects/claude-test/Ledger-Of-Alpha && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - WeeklyCalendar outer container has `rounded-md border dark:border-slate-800 border-slate-200`
    - Day popup uses `rounded-md`
    - Day cells use `rounded-md`
    - Weekly calendar visually matches the unified card styling
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- grep `rounded-2xl` in DashboardShell.tsx returns only time filter pills and utility group (UI controls, not cards)
- grep `shadow-sm` in DashboardShell.tsx returns zero results on card-like elements
- grep `rounded-xl` in WeeklyCalendar.tsx returns zero results on container/popup elements
- All card-like elements across dashboard use consistent `rounded-md border` styling
</verification>

<success_criteria>
- All widget cards use unified `rounded-md border` styling through WidgetCard component (CARD-01, CARD-03)
- No recent trades table widget exists (CARD-02 — already true, verified)
- WeeklyCalendar, hidden widgets panel, open trades, daily loss warning, empty state all match the new card style
- Visual consistency with the trades page design language
</success_criteria>

<output>
After completion, create `.planning/phases/26-top-bar-and-card-redesign/26-02-SUMMARY.md`
</output>
