# ─────────────────────────────────────────────
#  Ledger Of Alpha — Environment Variables
# ─────────────────────────────────────────────
#
#  Copy this file to .env.local for local dev:
#    cp .env.example .env.local
#
#  In Docker, pass these as environment variables
#  in docker-compose.yml or via -e flags.
# ─────────────────────────────────────────────

# ── Application ──────────────────────────────
# Public URL of your deployment (used in email links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── JWT Secret ───────────────────────────────
# Used to sign session tokens. Must be at least 32 characters.
# Generate one with: openssl rand -base64 32
JWT_SECRET=replace-this-with-a-real-secret-at-least-32-chars

# ── SMTP Email ───────────────────────────────
# Required for: email verification, 2FA OTP fallback, future notifications.
# If not configured, emails are printed to the console (dev mode only).
#
# Examples:
#   Gmail:    SMTP_HOST=smtp.gmail.com  SMTP_PORT=587  SMTP_SECURE=false
#             (use an App Password, not your account password)
#   Outlook:  SMTP_HOST=smtp.office365.com  SMTP_PORT=587  SMTP_SECURE=false
#   Mailgun:  SMTP_HOST=smtp.mailgun.org  SMTP_PORT=587
#   Local:    SMTP_HOST=localhost  SMTP_PORT=1025  SMTP_SECURE=false

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="Ledger Of Alpha <noreply@yourdomain.com>"
