// Chart UI is rendered persistently via PersistentChart in layout.tsx
// so it stays mounted (and TradingView keeps its state) across page navigations.
export default function ChartPage() {
  return null;
}
