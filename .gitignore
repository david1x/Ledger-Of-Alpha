# dependencies
node_modules/

# Next.js build output
.next/
out/

# database (local only — contains real trade data)
data/

# env files (never commit secrets)
.env
.env.local
.env.*.local
# .env.example IS committed — it has no secrets, only docs

# TypeScript build info
tsconfig.tsbuildinfo
next-env.d.ts

# OS
.DS_Store
Thumbs.db
